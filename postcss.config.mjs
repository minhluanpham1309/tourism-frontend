// Required for Tailwind CSS v4 with Next.js — without this file the Tailwind
// PostCSS plugin never runs and the app renders completely unstyled.
const config = {
  plugins: ["@tailwindcss/postcss"],
};

export default config;
